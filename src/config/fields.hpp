#pragma once

namespace rfxh::config {

    enum Field {
        F_OS, F_HOST, F_KERNEL, F_UPTIME, F_PACKAGES,
        F_SHELL, F_DISPLAY, F_WM, F_THEME, F_ICONS,
        F_FONT, F_TERMINAL, F_CPU, F_GPU, F_MEMORY,
        F_SWAP, F_DISK, F_IP, F_BATTERY, F_LOCALE,
        F_COLORS, F_COUNT
    };

} // namespace rfxh::config
